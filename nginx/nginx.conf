user  nginx;
worker_processes auto;
pid /run/nginx.pid;

events {
    worker_connections 2048;
}

http {
    include       /etc/nginx/mime.types;
    default_type  application/octet-stream;

    upstream django_medicase {
        server django_medicase:8000;
    }

    # redirect http -> https
    server {
        listen 80;
        server_name _;
        # server_name medicase-isfahan.com www.medicase-isfahan.com medicase-isfahan.ir www.medicase-isfahan.ir;

        return 301 https://$host$request_uri;
    }

    server {
        listen 443 ssl;
        server_name _;
        # server_name medicase-isfahan.com www.medicase-isfahan.com medicase-isfahan.ir www.medicase-isfahan.ir;

        ssl_certificate /etc/ssl/certs/medicase.crt;
        ssl_certificate_key /etc/ssl/private/medicase.key;

        ssl_protocols TLSv1.2 TLSv1.3;
        ssl_prefer_server_ciphers on;

        access_log  /var/log/nginx/access.log;
        error_log   /var/log/nginx/error.log;

        location /api/ {
            proxy_pass http://django_medicase;
            proxy_http_version 1.1;
            proxy_redirect off;

            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }

        location /static/ {
            alias /app/MediCaseBack/staticfiles/;
        }

        location /media/ {
            alias /app/MediCaseBack/media/;
            expires 30d;
        }

        location / {
            root /usr/share/nginx/html;
            index index.html;
            try_files $uri $uri/ /index.html;
        }
    }
}
